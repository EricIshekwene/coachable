/**
 * Main-app layout system barrel.
 *
 * Import page-layout primitives from here so the consistent page structure is
 * the path of least resistance:
 *
 *   import { AppShell, AppHeader, AppPage, AppSection, AppCard } from "../../components/layout";
 *
 * See LAYOUT.md for the hierarchy and when to use each component.
 */
export { default as AppShell } from "./AppShell";
export { default as AppHeader } from "./AppHeader";
export { default as AppPage } from "./AppPage";
export { default as AppSection } from "./AppSection";
export { default as AppCard } from "./AppCard";
