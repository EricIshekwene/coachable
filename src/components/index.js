// Main-app shared component barrel.
//
// Import recurring UI components from here rather than reaching for a raw
// page-local definition, e.g.:
//   import { PlayCard, FolderCard } from "../../components";
//
// Layout primitives live in their own barrel: `src/components/layout`.

export { default as PlayCard }   from "./PlayCard";
export { default as FolderCard } from "./FolderCard";
